---
- name: Клониреум репозиторий
  ansible.builtin.git:
    repo: "https://github.com/ahamitov/docker-demo-pub.git"
    dest: "{{ git_folder }}"
    version: master

- name: Собираем image
  community.docker.docker_image:
    name: "{{ registry_name }}{{ item.name }}"
    tag: "{{ item.version }}"
    push: true
    build:
      path: "{{ git_folder }}"
      dockerfile: "{{ git_folder }}/apps/{{ item.name }}/Dockerfile"
    source: build
  loop: "{{ services | difference(non_build_services) }}"

- name: Удаляем репозиторий
  ansible.builtin.file:
    state: absent
    path: "{{ git_folder }}"
